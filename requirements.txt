# Core Python libraries
numpy>=1.25
torch>=2.1.0

# Gradio for UI
gradio>=3.45

# Audio handling
soundfile>=0.12.1

# Text-to-Speech
TTS>=0.14.0
coqui-tts>=0.14.0

# Whisper for speech recognition
faster-whisper>=0.8.0

# LangChain
langchain>=0.1.0
langchain-community>=0.0.16

# Vector database and embeddings
faiss-cpu>=1.7.4
sentence-transformers>=2.2.2
langchain_ollama>=0.1.3

# Note: nvidia-cusparselt-cu12 was not found and thus not removed as it was already absent.

# FAISS for vector embeddings (RAG)
faiss-cpu>=1.7.4

# Sentence Transformers for embeddings (Granite)
sentence-transformers>=2.2.2

# Optional: for caching, threading, and utilities
requests>=2.31
pydantic>=2.6
